import TopicStub from "@/components/layout/TopicStub";
export default function Page() {
  return <TopicStub subject="Neural Networks" subjectPath="/neural-networks/backpropagation" topic="Backpropagation" preview="Step through forward and backward passes, seeing gradients flow through a small network." />;
}
