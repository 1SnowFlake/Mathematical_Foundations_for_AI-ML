import TopicStub from "@/components/layout/TopicStub";

export default function SVDPage() {
  return (
    <TopicStub
      subject="Linear Algebra"
      subjectPath="/linear-algebra/svd"
      topic="Singular Value Decomposition"
      preview="Decompose any matrix into rotation, scaling, and rotation — visualized step by step."
    />
  );
}
